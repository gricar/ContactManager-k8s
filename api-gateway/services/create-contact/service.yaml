apiVersion: v1
kind: Service
metadata:
  name: create-contact-svc
  labels:
    app: contact-api
spec:
  selector:
    app: contact-api
    service: create
  type: ClusterIP
  #type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      #nodePort: 31002
